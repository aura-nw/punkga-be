table:
  name: user_personal_wallet
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - user_id
        - chain_id
        - id
        - address
        - created_at
        - updated_at
      filter: {}
  - role: user
    permission:
      columns:
        - user_id
        - chain_id
        - id
        - address
        - created_at
        - updated_at
      filter: {}
