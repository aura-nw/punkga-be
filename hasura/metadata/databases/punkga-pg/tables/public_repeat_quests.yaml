table:
  name: repeat_quests
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - quest_id
        - created_at
        - updated_at
      filter: {}
  - role: user
    permission:
      columns:
        - id
        - quest_id
        - created_at
        - updated_at
      filter: {}
