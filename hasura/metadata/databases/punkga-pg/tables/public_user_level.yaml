table:
  name: user_level
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - user_id
        - level
        - xp
      filter: {}
  - role: user
    permission:
      columns:
        - user_id
        - level
        - xp
      filter: {}
      allow_aggregations: true
